language: haxe

os:
 - linux

# since 3.2.1 is what i have on my own laptop.
haxe:
 - "3.2.1"

install:
 - haxelib install flixel > /dev/null
 - haxelib run openfl setup > /dev/null

script:
 - cd Flixel && make build-neko



